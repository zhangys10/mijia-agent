{
  "name": "mijia-agent-makers-adapter",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.13.0" },
  "scripts": {
    "build": "node scripts/sync-python.mjs",
    "test": "npm run build && node --experimental-strip-types --test tests/*.test.mjs"
  }
}
